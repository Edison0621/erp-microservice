apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-service
  namespace: erp-system
  labels:
    app: finance
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: finance
  template:
    metadata:
      labels:
        app: finance
        version: v1
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "finance-service"
        dapr.io/app-port: "80"
    spec:
      containers:
        - name: finance
          image: erp-system/finance:latest
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: erp-config
            - secretRef:
                name: erp-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: finance-service
  namespace: erp-system
spec:
  selector:
    app: finance
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
