apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: erp-system
  labels:
    app: inventory
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory
  template:
    metadata:
      labels:
        app: inventory
        version: v1
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "inventory-service"
        dapr.io/app-port: "80"
    spec:
      containers:
        - name: inventory
          image: erp-system/inventory:latest
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: erp-config
            - secretRef:
                name: erp-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: erp-system
spec:
  selector:
    app: inventory
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
